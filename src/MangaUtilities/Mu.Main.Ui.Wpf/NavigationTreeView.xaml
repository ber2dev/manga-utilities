<ui:UserControlBase 
    x:Class="Mu.Main.Ui.Wpf.NavigationTreeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="clr-namespace:Mu.Client.Wpf.Infrastructure.Ui;assembly=Mu.Client.Wpf.Infrastructure"
    xmlns:hierarchies="clr-namespace:Mu.Core.Common.Hierarchies;assembly=Mu.Core"
    mc:Ignorable="d"
    Width="300">
    <Grid>
        <TreeView x:Name="TreeView" MouseDoubleClick="TreeView_OnMouseDoubleClick">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type hierarchies:Node}" ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Data.BitmapImage, Mode=OneTime}" />
                        <TextBlock Text="{Binding Data.Label, Mode=OneTime}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</ui:UserControlBase>
